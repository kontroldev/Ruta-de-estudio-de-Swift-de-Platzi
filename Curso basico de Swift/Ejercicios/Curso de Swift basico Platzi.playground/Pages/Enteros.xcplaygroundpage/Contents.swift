import UIKit

let age = 31

//Esto es para no desperdiciar memoria, se usa de la siguiente forma:⬇️

let minValue = UInt8.min  // 2^0-1 dos elevado a cero menos 1  (es el mas pequeño)
let maxValue = UInt8.max  //2^8-1  dos elevado a 8 menos 1 (es el mas grande)
let minValue16 = UInt16.min
let maxValue16 = UInt16.max
let minValue32 = UInt32.min
let maxValue32 = UInt32.max
let minValue64 = UInt64.min
let maxValue64 = UInt64.max


